resources:
  pipelines:
    foreach_batch_sink_demo:
      name: "ForeachBatch sink demo"
      catalog: workspace
      schema: default
      serverless: true
      channel: CURRENT
      continuous: false
      libraries:
        - file:
            path: ../src/flows_and_configuration/foreach_batch_sink_demo.py
