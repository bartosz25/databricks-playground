# This is a Databricks asset bundle definition for continuous_trigger.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: incremental_refresh_demos
  uuid: 5b0eb41b-f1bd-4add-9bc6-2591e3b645bf

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --sdist --wheel


targets:
  dev:
    mode: development
    default: true
    workspace:
      host: YOUR WORKSPACE
