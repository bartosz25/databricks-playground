resources:
  pipelines:
    broken_append_partition_row_demo:
      name: "Broken append-only demo"
      catalog: workspace
      schema: default
      serverless: true
      channel: CURRENT
      continuous: false
      libraries:
        - file:
            path: ../src/incremental_refreshes/broken_append_partition_row_demo.py
