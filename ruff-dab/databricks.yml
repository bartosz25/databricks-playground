# This is a Databricks asset bundle definition for poe_demo.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: ruff_dab
  uuid: 5b0eb41b-f1bd-4add-9bc6-2591e3b645bf

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: poe build_valid_wheel


targets:
  dev:
    mode: development
    default: true
    workspace:
      host: XXXX
